# 第二轮代码实施交接

日期：2026-09-14。依据评审后的规格 v3.2 完成本节点代码实现，交下游 #4 代码评审。本记录不替代独立评审，也不宣称研究效果通过。未执行 `git add` 或 `git commit`。

## 实现结果

- 新增因子级 QR/SVD 谱投影与有效权重插值，以 rank=128、谱裕量、固定五个 α 接入整组事务；沿用原求解器，补齐实际前向偏差、拒绝原因及恢复。
- 新增 v3.1 父 study、trial、method/policy/seed 和预注册阶段身份，保留旧版 hash/读取路径。R1/R2 共用阶段总账，试点锁永久 pilot_only，放行依据原生文件重新计算。
- 双卡研究与 Pro 6000 新运行均要求进展/资源证据；成本覆盖独立求解器及必经分支，零更新与比较导出状态分开报告，恢复按剩余快照空间预检。
- 完整词表评分按精确因果位置分块，避免持有完整 prompt 的 CPU 存储；新增固定单侧配对语义区间及三 seed 汇总，并修正 recovery 声明。

使用步骤和精确接口见 [运行手册](RUNBOOK.md)，交付范围见 [逐文件清单](file-plan.md)，机器可读核对见 [verification.json](verification.json)，原始字节摘要见 [SHA256SUMS](SHA256SUMS)。规格中已补充实施发现及边界。

## 验证结果

| 验证 | 结果 | 证据 |
| --- | --- | --- |
| 完整 CPU 回归 | 250 项通过，退出 0 | [full-cpu-final.log](full-cpu-final.log) |
| 真实子进程超时与后代回收 | 1 项通过，无关进程存活 | [local-budget.log](local-budget.log) |
| Linux/CLI 入口 | 4 个模块帮助、2 个 bash 语法、1 个准备脚本帮助通过 | [entrypoints-final.log](entrypoints-final.log) |
| Python 编译、文件及增量函数约束 | 43 个文件通过 | [static-final.json](static-final.json) |
| 源码增量嵌套深度 | 25 个源码文件、172 个增量函数，最大 4 层 | [nesting-final.json](nesting-final.json) |
| Ruff | 43 个任务 Python 文件通过 | [verification.json](verification.json) |

合计 **251 项不同测试**。最终 CPU 回归使用开发服务器现有环境，将当前工作树源码及测试传到进程内存加载，显式隐藏 CUDA；未覆盖远端项目源码、未安装新依赖。Python 3.12.13，torch 2.13.0，transformers 5.16.1，peft 0.20.0，optuna 4.9.0，lm_eval 0.4.12。真实子进程测试在本地运行，补足内存加载器无法支持的新解释器导入场景。全部测试输入为人工夹具。

中间失败日志完整保留。事务回归暴露的身份 mock、旧模板假设及验证器缺失 `__file__` 已修正，并以最终全量及入口复验为准；早期静态超限已经在计划内模块按职责拆分。任务平台曾拒绝 `wait_async` 的 file_exists 探针，原检查随后正常结束，该记录未被计为检查通过。

## 研究边界与交接关注点

本轮未运行新的真实 GPU pilot、27B NF4 模型验收、扩大搜索、能力审计、人工双评或 `tests/README.md` 的模型下载/checksum 流程。此前五组零更新诊断及基线结论保持原样；本目录没有伪造的资源测量或 readiness。是否超过基线仍需按 R1→R2→R3 取得真实证据。

旧 P2 中预测误差接线、剩余快照预检、阶段成本检查点和 recovery 标签已有对应实现与回归；异常退出时刻仍采用标注为 `stop_estimated` 的保守计费。嵌套 GPU 测量使用外层峰值上界，不能解释为独立子操作峰值。

建议评审重点复核：共享 campaign 在不同运行根目录并发启动同一正式成员时的协调边界；异常回溯/资源调用的部分测量持久化完整性；原生阶段账本与成本来源的全链关联。正常路径、文件篡改、缺证据和恢复测试已覆盖，但这些集成边界仍需真实执行复核。资源操作失败会保留阶段失败，不能据不完整成本放行。

原有 23 项暂存删除及其他论文/运行时材料保留，提交节点必须按本轮文件清单精确选择，不整体暂存工作区。新单卡 launcher 以提交内容归档源码，须在下游评审及提交后才使用本实现开展真实实验。
