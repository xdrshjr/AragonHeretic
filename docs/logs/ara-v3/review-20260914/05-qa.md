# 代码诊断 QA 独立仲裁

日期：2026-09-14。角色：最终 QA 仲裁。依据：`docs/plans/ara-v2-refusal-optimization/spec.md` v3.2；新增制品仍使用规格定义的 v3.1 schema。本文区分“原发现是否成立”和“修复是否已经验收”，不能因正在修复而把真实发现改判为误报。

**最终结论：本轮代码评审通过，可以提交本轮实施变更。八项 P1 全部修复并关闭，open P0=0、open P1=0、open P2=2。** 原发现仲裁为 CONFIRMED 10 项，其中 P1/HIGH 8 项、P2/MEDIUM 2 项；REJECTED 0 项，NEEDS_INVESTIGATION 0 项，P0/CRITICAL 0 项。原发现的 CONFIRMED 状态不因修复而改变。两项 P2 保留为后续待办，限制见对应条目；本结论不代表 GPU 实验、研究效果或整项研究目标已经通过。

## 输入与独立性

- Scanner：`review-20260914-scan-numerics.md`、`review-20260914-scan-gates.md`、`review-20260914-scan-identity.md`。
- Reviewer：`review-20260914-independent-review.md`。Reviewer 对 SCAN-GATE-004/005 作范围修正，其余确认，没有追加独立编号。
- QA 独立读取规格及引用源码，核对真实调用边界、恢复语义和当前修复；源码由其他代理同时修改，本文中的原缺陷定位以 Scanner/Reviewer 快照为准，当前修复另列。没有修改源码、测试或 Git 索引，没有访问服务器、凭据或任务平台，没有派生代理。
- QA 本地重跑 `test_ara_research_acceptance.NewResearchArtifactTests`：5 项通过，0.043 秒；另通过真实 `load_bound_acceptance`、`verify_research_artifact_graph` 和临时制品执行 12 项绑定断言，全部通过。模型和核心权重为测试夹具，不构成研究结果。
- 查阅主代理 `docs/logs/ara-v3/review-20260914/memory-before.log`、`memory-after.log`、`identity-binding.log`、`identity-protocol.log`。这些日志是主代理执行证据，未计为 QA 自行运行；最后一份为 43 项通过。
- 最终复核另查阅 `full-cpu-final.log`：24 模块 CPU 回归共 **273 项通过**，2.413 秒，退出码 0；`local-budget.log`：真实子进程预算测试单独 1 项通过，1.339 秒；`source-version-before.log`/`source-version-after.log`：旧版、未知版及缺版本来源协议的三个负例修复前失败，修复后协议模块 14 项通过。没有重复执行全量，也没有把先前 `gates-integration.log` 的中间失败误计为最终通过。
- 最终 `static-validation.json` 已核对为通过：47 个计划路径、43 个 Python 文件均完成静态核验，暂存字节及 HEAD 未改变。最后的函数抽取将阶段规则集中于 `_verify_stage_call`，QA 重读确认逻辑不变；三个阶段完成性回归最终位于既有计划文件 `tests/test_pro6000_experiment.py:StageCompletionRegressionTests`，并纳入上述 273 项最终运行。
- QA 最终独立执行两个实际 Python 子进程竞争同一成员：仅一个目录成功领取，另一目录拒绝；原目录继续看到已累计 3 秒，超时标记使后续成员被拒绝并将 `blocking_failure` 写入 campaign。另使用原测试文件 AST 原样抽取纯夹具函数、调用真实 `pilot_cost_prediction`，合法完整成本为 41.25 秒；五项成本分别改指同一已计费来源中的 capture 记录，全部因实际操作错误被拒绝，来源文件字节保持不变。AST 仅避免测试模块顶层 torch 导入，不替换生产函数。
- 最后阶段完成性修复另有 `resource-completion-before.log`/`resource-completion-after.log`：3 项新回归修复前 2 个失败、1 个错误；修复后连同成本与来源回归共 17 项通过。QA 独立原反例复验：必需 anchor2 资源 OOM 被拒绝，未被计费用作来源的普通 comparison 数值 FAIL 仍允许 41.25 秒预测，同 comparison 改成 OOM 则拒绝。这里的秒数为夹具，不是 GPU 测量。

## 逐项最终裁定

### NUM-001：阶段重置清除已发生的显存超限

- **状态：CONFIRMED；最终严重度：P1 / HIGH；优先级：IMMEDIATE。**
- Scanner 与 Reviewer 一致。QA 核对 `_block_transaction` 中 `monitor_before → capture → local_optimization` 顺序，以及 `measure_research_phase` 的顶层 CUDA reset：原实现记录前一阶段峰值后未核验，后续 guard 读取可被 reset 的计数器。23 GiB 峰值在下一阶段变成 6 GiB 不能说明遵守了 22 GiB 上限。符合规格 259、269、632 行关于资源故障中止 attempt、完整恢复及真实峰值的要求。
- 可操作措施：必须在下一次 reset 前执行已采集峰值约束，保持嵌套测量不清零及已有异常传播；与 SCAN-GATE-005 联合核验资源故障不得变成普通效果失败。
- **修复验收通过，可以关闭。** 当前 `ara_refinement_capture.py:485–512` 在 finally 保存 record 后，对正常返回阶段执行 `_check_phase_peak`，超限即标 failed 并抛错，因而不会进入下一阶段。异常阶段保留原异常。`memory-before.log` 两项中一项失败，准确复现原缺陷；`memory-after.log` 13 项通过，包括阶段序列、嵌套原异常及全 trial 恢复。最终 `full-cpu-final.log` 同时覆盖资源故障停止与恢复；`_blocking_trial` 识别显存上限异常，使其向 campaign 层传播。CUDA 计数器使用桩，不是 GPU 容量实测。

### NUM-002：异常丢失回溯和已完成层组证据

- **状态：CONFIRMED；最终严重度：P2 / MEDIUM；优先级：SHORT_TERM。**
- Scanner 与 Reviewer 一致。QA 确认 `ara_backtracking.py:258–270` 在 finally 恢复并填写局部 row，但异常绕过 return；调用方 287–288 行只在正常返回后追加 row。`ara_refinement.py:428–436` 和 471–497 行同样只接收正常返回事件并持久化 COMPLETE；因此 monitor、预算等异常会失去已经计算的 alpha、guard、真实误差、恢复身份和先前事件。规格 547 行明确要求这些证据。
- 不升至 P1：未发现这项直接造成错误接受，A/B 与全 trial 恢复路径存在。缺陷在失败证据持久化，而非已证明的恢复失效。
- 可操作措施：进入尝试即登记 row/event；异常边界补齐精确原因和恢复身份并原子保存 FAIL 证据，继续抛原异常。回归同时断言完整恢复及证据仍可读取。
- **尚未修复验收，可列后续待办。** 在补齐前不得声称每次昂贵失败均已有完整可审计轨迹。

### SCAN-GATE-001：实际计费阶段与必经阶段覆盖不一致

- **状态：CONFIRMED；最终严重度：P1 / HIGH；优先级：IMMEDIATE。**
- Scanner 的真实函数反例与 Reviewer 的调用分析一致。QA 核对 `pilot_cost_prediction → _measured_cost → _measurement_seconds/_validate_measured_branches`：原实现只检查 observation 自述与所选记录相符，却从整份 source 搜索覆盖阶段。其他未计费位置存在完整 trial/replay/export，不能使一条极短 capture 成为相应完整成本。
- 规格 604 行允许最大观测值乘工作量倍率的保守估计，但基础计时必须属于冻结公式中的实际操作；问题不在于公式没有数学上的最坏上界。
- 可操作措施：约束每项实际计费 phase，trial 覆盖同一完整调用的必需分支；两次 replay 均有有效测量，第三次 apply、reload、export 分别纳入。对五类成本分别替换成 capture，保留来源正确摘要仍须拒绝；合法完整观测保持原公式通过。
- **修复验收通过，可以关闭。** `_validate_measured_branches` 核对每种成本实际引用的 phase 集合；`_validate_cost_operation` 再核对登记 operation、对应来源文件及固定计时路径。整 trial 来源中的分支检查与该实际完整调用绑定，两个 replay 成本取观测最大值后按公式乘二，apply/reload/export 分项取最大值再求和，shortlist 至少覆盖三个成员。QA 独立五项 capture 负例及原生完整计费正例均通过；没有用其他 source 未计费位置的 phase 名称替代实际计费。

### SCAN-GATE-002：成本来源未与目标、完整校准及 R2 计费绑定

- **状态：CONFIRMED；最终严重度：P1 / HIGH；优先级：IMMEDIATE。**
- Scanner 与 Reviewer 一致。QA 确认原 `_measurement_seconds` 只检查来源 execution 的自有摘要与 method/policy；自身 hash 正确证明内部一致，不能证明是该目标硬件、full-calibration 和已计费 StageExecution。`build_pilot_readiness` 分别核验预算账本及资源清单，不建立每条 observation 到已登记调用的关系。规格 500、502、549、602、604 行均要求这条联系。
- 可操作措施：资源验证接收目标契约和共享 R2 账本；从 source/source_trial 追溯原协议、完整角色身份、真实设备、预注册调用、终态、evidence_hash 及实际计费。改变目标、profile、设备或执行 ID，并同步重算合法文件摘要后仍须拒绝。
- 合法边界：已预注册的 R2 seed42 资源观测可以用于目标 seed43/44 的估计；来源 pilot 与正式 protocol 总 hash 本就不同。应核验共同 target 和明确映射，不能要求来源已实际跑过每个目标 seed 或要求两协议总 hash 相同。
- **修复验收通过，可以关闭。** 当前成本链已接收同目标已结清 R2 账本，每个 source 必须唯一属于一个已完成调用；核验登记参数、预约区间、source protocol、真实完整校准角色、trial/evidence hash、父执行身份、method/policy 和原生计时 operation。reload 生成端已包含 `execution_identity`、`measurements.reload`，资源汇总同时绑定 `source_trial`，来源协议经共同目标字段与明确 v3.1 版本校验。缺调用、未计费、来源漂移和短阶段负例已覆盖。
- **最终 QA 查出并推动修复的同一问题边界：** 冻结目标额外预注册 `r2-extra-narrow-resource`（复制 anchor0 参数，purpose=resource）后，由该调用提供完整合法成本；此前将必需 `r2-backtrack-anchor2-resource` 的账本终态置为 `FAIL / OutOfMemoryError / CUDA out of memory`，真实 `validate_target_contract` 与 `pilot_cost_prediction` 仍接受并预测 41.25 秒。未修改生产函数，source/trial/调用引用仍按合法夹具构建；失败的必需 anchor2 没被引用，因此逐 observation 的 COMPLETE 校验没有挡下。规格 599、602、604、555/641 行不允许用另一较窄资源调用掩盖必需探针的资源故障。
- **该边界已关闭：** `_verify_stage_ledger` 调用 `_verify_stage_call`，对 progress/resource/replay 必需调用要求 COMPLETE；普通 comparison 的效果/数值 FAIL 仍按冻结规则保留，但 OOM/设备/身份故障经 `_blocking_trial` 阻断。`StudyBudget._fail_stage` 在异常及硬超时路径保存失败类别和原因，避免消费者只能看到无分类的 FAIL。修复前 3 项回归真实失败，修复后相关 17 项通过；QA 原反例独立重跑转为拒绝，普通 comparison 数值 FAIL 正例仍通过、comparison OOM 负例拒绝。没有扩大为“所有 FAIL 一律阻断”，也没有新增重复编号。

### SCAN-GATE-003：相对账本路径按启动目录重新发放阶段预算

- **状态：CONFIRMED；最终严重度：P1 / HIGH；优先级：IMMEDIATE。**
- Scanner 与 Reviewer 一致。原契约合法接受 `ledger_path="R1.json"`，`phase_budget_path` 直接构造 Path，在不同 CWD 得到不同账本及不同锁。共享预算和 StageExecution 去重仅在各自文件内有效，违反规格 483、602、639 行。
- 可操作措施：冻结阶段使用稳定唯一的绝对账本定位，或固定 campaign 根解析后纳入目标摘要；不得再依赖启动 CWD。R1/R2 账本不能混用。测试相对路径拒绝、不同 CWD 的同一绝对定位，以及历史调用不会再领取完整额度。
- **修复验收通过，可以关闭。** `frozen_ledger_name` 拒绝相对路径和父目录跳转，目标摘要冻结规范路径；R1/R2/search/experiment 路径重复被拒绝，实际执行另要求该路径在本机可解析为绝对路径。既有跨 run_dir 共享阶段预算测试、Linux 绝对路径/相对路径验证及累计恢复回归均通过。不同 CWD 无法再解析出两份新阶段总账。

### SCAN-GATE-004：同一预注册正式成员换目录重新领取 study 预算

- **状态：CONFIRMED；最终严重度：P1 / HIGH；优先级：IMMEDIATE。**
- 采纳 Reviewer 的范围修正：问题是**同 target 下同一预注册成员**，不是禁止全部独立 experiment。原 admission 不持久占位，primary seed42 可直接返回；各 root 自带预算和锁，完成结果可按 member 覆盖。换 root 可重复 24 attempts 或完整小时额度，违反规格 489、600、620、622 行。
- 可操作措施：campaign 锁内原子登记成员、study hash、canonical run_dir、预算路径及状态；在模型分配前完成。另目录的同成员申请拒绝或只读返回既有结果；原目录恢复必须保留累计预算和已完成操作。
- 合法边界：不同预注册成员各有自己的 study 额度；明确冻结的新目标或独立 experiment 可有各自预算。不能为了去重而关闭原目录的合法恢复。
- **修复验收通过，可以关闭。** `reserve_campaign_member` 在 campaign 文件锁内保存 study hash、规范 run_dir、预算路径和 RUNNING；换目录拒绝，同目录须保有原预算及已登记 study。`record_campaign_study` 防止已开始成员删掉 study 重新获得 attempts。双卡 `run_budgeted_phase` 和单卡 `_run_search` 均先进入 `campaign_member` 再创建预算/模型，已有根目录 worker 锁继续生效。已完成记录保留原 binding，因此完成后另目录也不能重领；正式已有 staging 可只读恢复。QA 额外真实双进程竞争及累计恢复通过，已有删除预算/study、不同成员、零更新基线正例回归通过。

### SCAN-GATE-005：资源或身份故障被写为可继续扩大的 COMPLETE

- **状态：CONFIRMED；最终严重度：P1 / HIGH；优先级：IMMEDIATE。**
- 采纳 Reviewer 的分类修正。原 runner 可将 OOM/设备 RuntimeError 记普通 FAIL，campaign 却无条件写 COMPLETE；没有 COMPLETE trial 时 `all_zero_update=False`，使后续目标 seed 得到 admission。另有 replay/staging 前写 COMPLETE 的窗口。规格 259、269、555、641 行均不允许资源或身份故障享受普通基线效果失败豁免。
- 可操作措施：保留足以判定的故障类别并在 campaign 中锁存阻断原因；必需 replay/staging 完成后提交最终状态，异常要留下跨目录可见的失败/未运行记录。与 NUM-001 合验显存上限异常真实中止并恢复。
- 合法边界：正常 B1/B2/S1 全零或纯效果未达标仍可作为 comparison_only/unavailable，不能阻断其他已获准成员；普通数值失败按冻结 attempt 健康规则处理，不能仅凭任意 FAIL 或 RuntimeError 就认定共享资源故障。目标全零只停止后续目标 seeds。
- **修复验收通过，可以关闭。** 新版 `_run_attempt` 记录资源/身份故障后抛出，`campaign_member` 持久锁存 FAIL/阻断原因；`_finish_search` 仅在 replay/staging 完成后写 campaign 完成状态，单卡同样在结果形成后提交。普通数值 FAIL 的冻结健康规则、基线全零继续及目标全零只停止后续目标 seeds 均保留。watchdog 的 `os._exit` 不依赖 Python 异常展开：先写 `budget-timeout.json`，下一次任何成员 admission 通过 `_latch_campaign_timeouts` 扫描登记预算路径并将失败持久写回 campaign。真实 watchdog 子进程测试、非主成员 timeout、OOM、replay 失败及正常数值/基线正例均有通过证据；QA 又独立验证 marker 导致跨成员持久阻断。

### IDENTITY-001：共同目标没有冻结真实评分、精度和正文身份

- **状态：CONFIRMED；最终严重度：P1 / HIGH；优先级：IMMEDIATE。**
- Scanner 的公开构建入口反例与 Reviewer 一致。原 target 严格字段缺量化/精度，scorer_identity 没有对照实际 judge_identity，角色只比 ID。因此各自 hash 正确的协议可在同 target 下改变执行条件；新 protocol hash 不同本身并不构成拒绝依据。规格 215、500、502、549 行要求共同 development 内容、评分及目标环境一致。
- 可操作措施：target 固定精度、完整可执行 scorer 身份和各 profile 的有序题目、正文/system、来源摘要；构建、执行前以及 readiness 来源消费均核验。R1/R2 的 fit/monitor 合法映射与不同 trial 的 adapter 差异必须继续允许。
- **修复验收通过，可以关闭。** target 已新增 `quantization`、`dtype`、`role_identities`；共同验证器绑定实际 judge 与 scorer，核验角色摘要及跨 profile 共同开发正文。准备端、执行前、`ara_pilot._validate_source_context` 和成本来源消费端均使用该验证器，关闭 QA 第一轮指出的来源消费缺口。验证器起点明确要求 PROTOCOL_SCHEMA_V31，旧/未知/缺版本的来源即使重新计算正确 hash 也不能解释成新目标证据。
- `identity-protocol.log` 43 项通过，覆盖正常公开构建、评分/精度/正文/来源漂移、开发正文跨 profile 变化和硬件误配；来源消费回归在更新 source/trial 协议摘要后仍拒绝评分/精度变化。`source-version-before/after.log` 提供三个版本负例的失败→通过证据，最终 273 项回归通过。合法 fit/monitor 映射与不同候选 adapter 身份没有被强制合并。

### IDENTITY-002：acceptance/reproduce/candidate 版本绑定不对称

- **状态：CONFIRMED；最终严重度：P1 / HIGH；优先级：IMMEDIATE。**
- Scanner 与 Reviewer 一致。旧 report 加新版 reproduce 可绕过原先只在新 report 分支中的版本/执行 hash 校验；新增字段存在但为空也未被拒绝。规格 494、559、642 行要求精确新旧分派及身份绑定，旧文件完整性 hash 不能消除跨代语义混用。
- **修复验收通过。** 当前 `validate_research_binding` 无条件比较 report/reproduce 版本；`_validate_passed_evidence` 核对 candidate/report 版本；新 reproduce 执行/父 study hash 要求非空，并与新候选逐项比较。
- 证据：`identity-binding.log` 的五项回归和 QA 独立本地重跑均通过。后续 `identity-protocol.log` 已包含“新版 report + 旧 reproduce”的通用加载负例及新版本 round-trip。QA 额外使用真实通用加载器与整图校验执行 12 项断言：旧/新合法正例、两个跨代方向、两个 hash 的空值与错配，以及旧/新文件读取前后字节不变，均通过。最初五项日志单独不能覆盖全部矩阵，补充证据已填补缺口。
- 后续措施：将通用入口双向混用负例长期保留在回归集；不回写历史制品或把旧 schema 升格解释。

### IDENTITY-003：机制双侧 bootstrap 缺少退化边界和分析身份

- **状态：CONFIRMED；最终严重度：P2 / MEDIUM；优先级：SHORT_TERM。**
- Scanner 与 Reviewer 一致。QA 确认 `paired_group_interval` 仅排除不足两组，恒定差值仍返回零宽数值区间；允许任意 seed，输出缺逐组配对值和重采样/分析身份。规格 331 行同时约束机制双侧及语义单侧分析。
- 不升至 P1：目前未见正式执行链调用此辅助函数，新 `paired_semantic_interval` 另有退化处理；不能据此声称已有正式语义优越性结果被错误放行。
- 可操作措施：为新机制分析明确版本，固定 seed/次数/线性分位数，对恒定差值、分布退化和身份缺失返回 inconclusive，并保存可核验配对值与抽样身份。保留历史结果字节；对非退化正例与退化负例分别测试。
- **尚未修复验收，可列后续待办。** 机制调用端正式使用或发表区间结论前必须补齐，不能把零宽区间当作优越性证据。

## 跨模块共同问题与优先顺序

1. **完整证据链必须核验语义归属。** 自有 hash、文件 hash、phase 名称和 COMPLETE 各自有效，不代表该文件属于共同 target、实际计费操作、已预注册调用或正确制品代次。目标→来源协议→调用/账本→trial→候选→验收/重现应逐边核验；生产端与消费端共用规则。优先合验 IDENTITY-001、SCAN-GATE-001/002 和已完成的 IDENTITY-002。
2. **逻辑身份应支配预算和生命周期。** CWD/run_dir 是存储定位，不应成为重新领取预算的理由。冻结账本定位、原子成员占位、累计预算、重放/完成提交必须共同工作。优先合验 SCAN-GATE-003/004/005 的正常恢复和拒绝重复两面，不能只测一方。
3. **恢复模型与保存失败事实是两件事。** NUM-001 解决峰值漏检，SCAN-GATE-005 要使该错误正确阻断，NUM-002 仍需保留已经计算的失败证据。失败不得降格成普通拒绝；也不能把正常基线效果失败升级成资源故障。

发现时点的范围内代码健康评为 **6/10**：数值主公式、事务恢复和新旧显式分派已有实质基础与测试，但八项 P1 集中影响研究放行真实性和累计预算。修复后本轮范围评为 **8/10**：全部 P1 已有明确关闭依据，来源消费与生产使用共同约束，预算占位和故障状态持久化形成闭环；仍保留两项 P2，且真实 GPU 运行尚不能由 CPU 夹具代替。该评分不是全仓无缺陷证明，也不是模型效果评价。下一步按原实施流程提交及开展后续阶段，NUM-002 的失败审计和 IDENTITY-003 的机制分析边界列入明确待办。

## 最终修复验收清单

| 发现 | 当前修复状态 | 最终关闭前的必要证据 |
| --- | --- | --- |
| NUM-001 | 已关闭 | 阶段峰值检查与资源异常中止/阻断通过；GPU 容量另属运行阶段 |
| NUM-002 | 待办，未验收 | monitor/预算异常后模型恢复且 FAIL 事件完整落盘 |
| SCAN-GATE-001 | 已关闭 | QA 五项错选短阶段均拒绝，完整来源调用计费正例通过 |
| SCAN-GATE-002 | 已关闭 | 成本来源/计费链通过；必需资源失败不能由另一探针替代，普通比较失败边界保留 |
| SCAN-GATE-003 | 已关闭 | 相对路径拒绝、共享固定定位及累计历史回归通过 |
| SCAN-GATE-004 | 已关闭 | QA 真实双进程竞争、原目录累计恢复、预算/study 丢失拒绝 |
| SCAN-GATE-005 | 已关闭 | 资源/身份/replay/硬超时锁存；正常基线和目标全零规则保留 |
| IDENTITY-001 | 已关闭 | 准备、执行、readiness/成本来源共同校验，精确版本拒绝 |
| IDENTITY-002 | 修复通过 | 已有源码审查、5 项回归、43 项关联回归及 QA 12 项独立断言 |
| IDENTITY-003 | 待办，未验收 | 新机制版本退化 inconclusive 和完整重采样身份 |

最终 open P0=0、open P1=0、open P2=2。**可提交本轮实施变更：代码行为验收、最终 273 项 CPU 回归、独立真实子进程预算核验及静态检查均已有通过证据。** 实际 Git 提交由下游 #5 按既有范围完成，本评审节点不执行 git add/commit。本 QA 没有执行源码或索引写操作。两项 P2 不构成本轮 P0/P1 关闭门槛，但失败事件完整性与机制区间声明仍受对应限制。CPU/纯逻辑回归仅证明其覆盖的实现行为；本 QA 没有执行 GPU 实验，也没有作 27B 模型效果、真实资源容量或研究达标声明。
